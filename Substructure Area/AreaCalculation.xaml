<Window x:Class="Substructure_Area.areaCalculation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Substructure_Area"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400" Title="Substructure Area" 
        WindowStartupLocation="CenterScreen" Topmost="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>

            <ColumnDefinition Width="20"/>


        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="20"/>

        </Grid.RowDefinitions>

        <Button x:Name="Elements_Selection" 
                Content="Select Element" Grid.Column="1"
                Grid.Row="1" 
                Click="Element_Selection_Click" 
                Height="20" MinWidth="80"  >
        </Button>

        <DataGrid x:Name="datagrid" 
                  AutoGenerateColumns= "True"
                  Grid.Row="2" Grid.Column="4" 
                   CanUserAddRows="False"        
                ScrollViewer.CanContentScroll="True"
                   AllowDrop="True" MinHeight="100" MinWidth="100"
                  Margin="50,0,0,0">
            <DataGrid.ContextMenu >
                <ContextMenu>
                    <MenuItem Header="Copy" Command="Copy"/>
                </ContextMenu>
            </DataGrid.ContextMenu>

        </DataGrid>

        <ListBox x:Name="Select_Family" Grid.Column="2"
                 Grid.Row="4" 
                 SelectionMode="Multiple" 
                 AllowDrop="True" Height="auto" 
                 Grid.ColumnSpan="2" >

            <ListBox.Resources>
                <Style TargetType="Table" >
                </Style>
            </ListBox.Resources>

        </ListBox>

        <Button x:Name="SaveAs" Content="Save As" Grid.Column="4" 
                Grid.Row="4" Click="Save_As_Click" 
                IsEnabled="{Binding ElementName=Select_Family , 
                    Path =SelectedItems.Count}">
        </Button>
        <Button x:Name="Cancel" Content="Cancel" Grid.Column="4" 
                Grid.Row="6" Click="Cancel_Click"  
                Grid.RowSpan="1"   >
        </Button>
    </Grid>

</Window>
